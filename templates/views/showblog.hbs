<!DOCTYPE html>
<html>
    <head>
        {{>link}}
    </head>
    <body>
        {{>header}}
        <div class="container-fluid px-3 py-4">
            <div class="card">
                <h1>{{post.title}}</h1>
                <h1>{{post.createdAt.toLocale}}</h1>
                <p>{{{post.sanitizedHtml}}}</p>
            </div>
        </div>
        <h1>Comments::</h1>
        <div class="container">
            {{#each post.comments}}
                <div class="card">
                    <h1>{{this.comment}}</h1>
                </div>
            {{/each}}
        </div>
        <div class="form-group">
            <form action='/blog/check/{{post.id}}' method='POST'>
    <label for="exampleFormControlTextarea1">Give a comment</label>
    <textarea class="form-control" name="comm" id="textarea" required></textarea>
  <button type="submit" class="btn btn-primary">Add</button>
  </form>
  </div>
        <a class="btn btn-primary" href="/blog">All articles</a>
    </body>
</html>